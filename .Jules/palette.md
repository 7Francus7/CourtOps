## 2024-05-22 - Accessible Form Labels
**Learning:** Inputs without explicit labels rely on placeholders, which is poor for accessibility and disappears on typing.
**Action:** Use explicit `<label>` elements with `htmlFor` and consistent styling (`text-xs font-bold text-text-grey uppercase tracking-wider`) to match the Login page pattern. This improves accessibility and visual clarity without breaking the minimalist design.
